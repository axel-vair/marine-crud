<nav class="navbar navbar-expand-lg" style="background-color: #e3f2fd;">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">Boat Lover</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav me-auto">
                <a {% if app.request.get('_route') == 'app_home' %} class="nav-link active"{% endif %} class="nav-link" aria-current="page" href="/">Accueil</a>

                <li class="nav-item dropdown">
                    <a {% if app.request.get('_route') == 'boat.index' %}  class="nav-link active" {% endif %} class="nav-link dropdown-toggle" href="/boat" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Navires
                    </a>
                    <ul class="dropdown-menu">
                        {% if is_granted('ROLE_ADMIN') %}
                         <li><a class="dropdown-item" href="{{ path("boat.add") }}">Ajouter un navire</a></li>
                        {% endif %}
                        <li><a class="dropdown-item" href="{{ path("boat.index") }}">Liste des navires</a></li>
                    </ul>
                </li>


                <li class="nav-item dropdown">
                    <a {% if app.request.get('_route') == 'classe.index' %} class="nav-link active" {% endif %} class="nav-link dropdown-toggle" href="/classe" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Classes
                    </a>
                    <ul class="dropdown-menu">
                        {% if is_granted('ROLE_ADMIN') %}
                            <li><a class="dropdown-item" href="{{ path("classe.add") }}">Ajouter une classe</a></li>
                        {% endif %}
                        <li><a class="dropdown-item" href="{{ path("classe.index") }}">Liste des classes</a></li>
                    </ul>
                </li>
            </div>
            {% if is_granted('ROLE_ADMIN') %}
                <a style="margin-right: 0.25rem" href="{{ path('admin') }}" class="btn btn-outline-primary" role="button">Admin</a>
            {% endif %}
            {% if app.user %}
                <div style="margin-right: 5rem" >

                    <a href="{{ path('user.edit', {id: app.user.id}) }}" class="btn btn-outline-primary" role="button">Profil</a>
                    <a href="/logout" class="btn btn-primary" role="button">Se deconnecter</a>
                </div>
            {% else %}
                <div style="margin-right: 5rem" >
                    <a href="{{ path('security.index') }}" class="btn btn-outline-primary" role="button">Se connecter</a>
                    <a href="{{ path('register.index') }}" class="btn btn-primary" role="button">S'inscrire</a>
                </div>
            {% endif %}

        </div>
    </div>
</nav>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>